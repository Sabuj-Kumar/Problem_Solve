#include<bits/stdc++.h>

using namespace std;

int main()
{
    long long input,store;
    cin >> input;
    store = ( (input * input) / 2) - (input / 2) + 1;
    cout << store << "\n";
    return 0;
}
