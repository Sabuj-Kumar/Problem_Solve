#include<bits/stdc++.h>
#define in freopen("in.txt","r",stdin);

using namespace std;

int main(){

    #ifndef ONLINE_JUDGE
    in
    #endif // ONLINE_JUDGE

    int test;

    cin >> test;

    while( test-- ){

        int n,s,a,b;

        cin >> n >> s >> a >> b;

        cout << min(abs(a-b)+s)-(n-1)) << endl;
    }
    return 0;
}
